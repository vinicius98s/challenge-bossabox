{
  "name": "server-mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx json-server db.json",
    "server": "concurrently \"npm run dev\" \"cd client && npm install && serve -s build\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}