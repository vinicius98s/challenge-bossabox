{
  "name": "server-mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently npx json-server db.json\" \"cd client && npm start\"",
    "client": "cd client && npm install",
    "server": "concurrently \"npm run client\" \"npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
