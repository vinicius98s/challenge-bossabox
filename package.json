{
  "name": "server-mock",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "cd client && npm install && npm run server",
    "dev": "npx json-server db.json",
    "client": "cd client && serve -s build",
    "server": "concurrently \"npm run client\" \"npm run dev\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.14.0"
  },
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}